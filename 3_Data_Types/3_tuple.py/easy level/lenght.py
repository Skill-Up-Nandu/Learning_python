# Tuple Length
# Write a program to print the number of elements in this tuple:
# colors = ('red', 'green', 'blue', 'yellow')

colors = ('red' , 'green' ,'blue' , 'yellow')
# calculate the length of tuple
length = len(colors)
print(f"Total Items In The Tuple are : {length}")
