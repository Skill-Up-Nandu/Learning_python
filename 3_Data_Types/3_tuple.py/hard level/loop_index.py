# Loop Through Tuple with Index
# Print each item in the tuple with its index: items = ("pen", "pencil", "eraser")

items = ("pen", "pencil", "eraser")

for idx , item in enumerate(items):
    print(f"Index {idx} : {item}")